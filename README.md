# TaxFlow Agent

This represents the frontend web application for TaxFlow Agent, built on React, Vite, and Tailwind CSS.

## Getting Started

1. Run `npm install` to install dependencies.
2. Run `npm run dev` to start the development server.
